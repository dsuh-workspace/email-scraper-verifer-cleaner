# Direct runtime dependencies for the lead-gen pipeline.
# Pin only what we actually import; let pip resolve transitives.

SQLAlchemy>=2.0,<3.0
python-dotenv>=1.0
requests>=2.31

# Google Sheets export path (falls back to CSV if these are missing,
# but Sheets won't work at all without them).
google-api-python-client>=2.100
google-auth>=2.20

# Test-only
pytest>=8.0
